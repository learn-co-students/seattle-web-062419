require 'bundler/setup'
Bundler.require

require_rel '../app'

# The lines above are just defaults that will require ALL gems in the GEMFILE.
# It's usually not needed if you only have a few dependencies with a single Class file.